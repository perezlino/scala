/*  ======================================================
    ========= SET NO ACEPTA ELEMENTOS DUPLICADOS =========
    ======================================================
*/

//  Notar que los Set de enteros no los devuelve de manera 'ordenada'.

    var numeros = Set(1,2,2,3,4,5,5,6,6,6,6)
    // var numeros: scala.collection.immutable.Set[Int] = HashSet(5, 1, 6, 2, 3, 4)

    var paises = Set("Chile","Chile","Bolivia","Peru","Peru","Argentina")
    // var paises: scala.collection.immutable.Set[String] = Set(Chile, Bolivia, Peru, Argentina)